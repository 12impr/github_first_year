#include <stdio.h>
int main()
{
	char n;
	printf("enter number");
	scanf("%c",&n);
	// int a= len(n);
	printf("the digit is %d",strlen(n));
	return 0;
	for (size_t i = 0; i < count; i++)
	{
		/* code */
	}
	
	
}